{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && zip -r .output.zip .output",
    "zip": "zip -r .output.zip .output",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.5",
    "bootstrap": "^5.1.3",
    "font-awesome": "^4.7.0",
    "particles.vue3": "^2.0.6",
    "tsparticles": "^2.0.6"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.1",
    "sass": "^1.51.0"
  }
}
